#!/usr/bin/env zsh

pkill yeahconsole
pkill urxvtd

urxvtd &
xrdb -load /home/krgr/.Xresources
yeahconsole &

for i in ~/.Xresources.yeah<->; do
    cat ~/.Xresources $i | xrdb -load && yeahconsole &
done
