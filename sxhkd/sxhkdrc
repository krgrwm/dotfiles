# Audio
XF86Audio{Mute,RaiseVolume,LowerVolume}
    amixer -q set Master {toggle, 2%+, 2%-}

# Brightness
XF86MonBrightness{Up,Down}
    xbacklight {+10,-10}

@Print
    ~/bin/screenshot

# dmenu
alt + {n,p,Return,d,n,e,y}
    ~/bin/dmenu_root ~/bin/dmenu/{launcher,window,edit,lookup,run,run_fav}
